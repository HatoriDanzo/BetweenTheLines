{
  "name": "between-the-lines",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio-quality AI recruitment intelligence tool for CV and public profile assessment.",
  "scripts": {
    "dev": "npx vercel dev",
    "serve": "npx serve public",
    "lint": "node --check api/analyze.js && node --check public/app.js",
    "test": "npm run lint"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "mammoth": "^1.11.0",
    "pdf-parse": "1.1.1",
    "sharp": "^0.35.0"
  }
}
